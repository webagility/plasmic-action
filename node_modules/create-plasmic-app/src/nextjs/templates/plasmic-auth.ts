export function makePlasmicAuth(plasmicHost?: string): string {
  // {
  //   "host": "${plasmicHost}",
  //   "user": "admin@example.com",
  //   "token": "${projectApiToken}"
  // }

  const text = `{
  "host": "${plasmicHost || "https://api.squirrel.dev"}"
}`;
  console.log('__DEV__', { text });
  return text;
}
