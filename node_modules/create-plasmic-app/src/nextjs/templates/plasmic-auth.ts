export function makePlasmicAuth(projectHost?: string): string {
  // {
  //   "host": "${projectHost}",
  //   "user": "admin@example.com",
  //   "token": "${projectApiToken}"
  // }

  const text = `{
  "host": "${projectHost || "https://api.squirrel.dev"}"
}`;
  console.log('__DEV__', { text });
  return text;
}
